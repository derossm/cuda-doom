if(WIN32)
	magic_add_executable("${PROGRAM_PREFIX}midiproc" WIN32 buffer.h main.cpp proto.h)
	target_include_directories("${PROGRAM_PREFIX}midiproc" PRIVATE "../src/" "${CMAKE_CURRENT_BINARY_DIR}/../")
	target_link_libraries("${PROGRAM_PREFIX}midiproc" SDL2::SDL2main SDL2::mixer)
endif()
